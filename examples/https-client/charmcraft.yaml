type: charm
bases:
  - name: ubuntu
    channel: "20.04"

parts:
  charm:
    build-packages:
      - cargo
      - git
      - libffi-dev
      - libssl-dev
      - rustc
    charm-entrypoint: src/https-client.py
    charm-python-packages:
      - setuptools < 58
