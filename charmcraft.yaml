type: charm
bases:
  - name: ubuntu
    channel: "20.04"

parts:
  charm:
    build-packages:
      - git
      - python3-dev
    charm-python-packages:
      - setuptools < 58
    # Avoid lengthy compilation of pyca/cryptography
    charm-binary-python-packages:
      - cryptography
  grafana-dashboard:
    plugin: dump
    source: .
    override-pull: |
      wget --https-only https://grafana.com/api/dashboards/15726/revisions/latest/download -O LXD.json
    build-packages:
      - wget
